let baseUrl = 'http://etrack.ws/pilot.svc/'; 
const urls = {
    loginUrl: `${baseUrl}authorization2?`,
    vehicleListUrl: `${baseUrl}devList/`,
    breadcrumbUrl: `${baseUrl}trail/`
};

export { urls };